#!/usr/bin/env clisp -ansi -q -Kfull -E iso-8859-1
;;;; -*- mode:lisp; coding:iso-8859-1 -*-
(setf *random-state* (make-random-state t))
(prin1 (random (let ((*read-eval* nil)) (read-from-string (elt ext:*args* 0)))))
(terpri)

